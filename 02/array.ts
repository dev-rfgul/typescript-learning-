const superHero: string[] = []
const heroPower: number[] = []
superHero.push("superman");
heroPower.push(2);

type user = {
    name: string;
    isActive: boolean;
}

const allUsers:user[]=[


]

allUsers.push({name:"fahad", isActive:true})